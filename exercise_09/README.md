# If, else if, and else statements

This exercise generates a random integer and uses an if - else if - else chain to categorize the value into different ranges. It practices conditional branching and the use of comparison operators in Go.
