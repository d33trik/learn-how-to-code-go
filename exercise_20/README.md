# If statement with initialization

This exercise uses a `for range` loop to run 100 iterations, each time generating a random integer and checking its value with an `if` statement that includes a short declaration. It demonstrates the `if` statement with an initialization statement and the `for range` loop with a numeric count in Go.