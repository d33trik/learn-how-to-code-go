# Interface implementation

This exercise demonstrates how to define an interface in Go and implement it implicitly with method definitions on different struct types. A `shape` interface is satisfied by both `square` and `circle` through their `area` methods, allowing a single function to accept any type that implements the interface.
