# Nested for loops

This exercise nests one `for` loop inside another to print the values of both loop variables for every combination. It demonstrates nested loop iteration and the interaction between outer and inner loop scopes in Go.