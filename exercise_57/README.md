# Sorting slices with the sort package

This exercise creates slices of integers and strings and uses `sort.Ints` and `sort.Strings` to sort them in place. It demonstrates how to sort built-in types using the `sort` package.
