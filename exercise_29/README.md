# Map with slice values

This exercise declares a map whose values are string slices and uses nested `for range` loops to print each key and its associated elements. It demonstrates composite literals for maps of slices and ranging over both map keys and slice indices in Go.
