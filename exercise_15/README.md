# For loop with continue

This exercise uses a `for` loop to iterate from 0 to 20 and prints only the odd numbers. It demonstrates the `continue` statement and the modulus operator for skipping loop iterations conditionally in Go.