# Add to a map

This exercise adds a new key-value pair to an existing map using map assignment and then iterates over the map with nested `for range` loops to print each key and its associated slice values. It demonstrates how to insert entries into a map in Go.
