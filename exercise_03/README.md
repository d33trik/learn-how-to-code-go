# Byte size constants with iota

This exercise demonstrates how to use `iota` with bit shifting to define byte size constants such as KB, MB, GB, etc. It practices constant declarations, typed constants, and binary representation formatting in Go.
