# The defer statement

This exercise demonstrates how to use the `defer` keyword to postpone a function call until the surrounding function returns. It covers the last-in, first-out execution order of deferred calls in Go.
