# Embedded struct types

This exercise defines an `engine` struct and embeds it within a `vehicle` struct, then creates two `vehicle` values and prints them. It demonstrates how to compose structs by embedding one struct type inside another in Go.
