# Slice index ranges

This exercise declares a slice of integers and uses slice expressions to extract and print sub-slices with different index ranges. It demonstrates the `s[low:high]` syntax for reslicing in Go.
