# Function wrapper

This exercise wraps a function with another function that measures its execution time. It demonstrates the wrapper pattern, where one function adds behavior around another by accepting it as a parameter.
