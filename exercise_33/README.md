# Struct type declaration

This exercise declares a custom `person` struct with `string` and `[]string` fields, creates two struct values using composite literals, and prints each field. It demonstrates defining composite types with `struct` and iterating over a slice field with `for range` in Go.
