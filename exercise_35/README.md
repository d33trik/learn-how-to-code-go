# Anonymous struct

This exercise declares an anonymous struct value with fields of different collection types and prints it. It demonstrates how to define and initialize a struct type inline without a named type in Go.
