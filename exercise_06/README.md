# Print integers in decimal, binary, and hex

This exercise declares three integer values and prints each one in decimal, binary, and hexadecimal format using `fmt.Printf` formatting verbs. It introduces the `%d`, `%b`, and `%x` verbs for number formatting.
