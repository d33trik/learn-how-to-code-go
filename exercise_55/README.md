# Unmarshaling JSON into structs

This exercise defines a `Person` struct with JSON struct tags and uses `json.Unmarshal` to deserialize a JSON string into a slice of structs. It demonstrates how Go maps JSON fields to struct fields using the `encoding/json` package.
