# Array capacity and integer range

This exercise declares an array of integers and uses `cap()` with `range` to assign each element its index value, then iterates over the array to print its contents. It demonstrates the `cap` function for arrays and ranging over an integer expression in Go.
