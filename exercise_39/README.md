# Methods with value receivers

This exercise demonstrates how to define a method on a struct using a value receiver in Go. A `person` struct declares a `speak` method that accesses the receiver's fields and prints a formatted message.
