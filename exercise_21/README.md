# Array declaration with ellipsis

This exercise declares an array of ice cream flavor names using the ellipsis `...` syntax to infer the array size from the literal, then prints its length with `len()` and its contents. It introduces array literals and size-inferred array declaration in Go.
