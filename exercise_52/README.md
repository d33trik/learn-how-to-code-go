# Struct methods and interfaces

This exercise defines a `dog` struct with `walk()` and `run()` methods and a `youngin` interface that requires both. It demonstrates how a struct satisfies an interface implicitly by implementing its methods, and how to pass concrete types to functions that accept interfaces.
