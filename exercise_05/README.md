# Print values and types

This exercise declares variables of type `string`, `int`, and `float64`, then prints their values and types using formatted output.

It practices variable declaration and the use of Go's `fmt.Printf` formatting verbs.
