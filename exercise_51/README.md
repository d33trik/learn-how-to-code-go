# Pointer declaration and dereferencing

This exercise declares pointer variables of type `*string` and `*int` at package level and assigns addresses to them in `init()`. It demonstrates how to dereference pointers with `*` to access their underlying values and how to inspect pointer types with `fmt.Printf`.
