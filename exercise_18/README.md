# Range over a map

This exercise declares a map with string keys and integer values and uses a `for range` loop to print each key-value pair. It demonstrates map literals and the `range` keyword for iterating over maps in Go.