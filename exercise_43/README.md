# Mocking with interfaces

This exercise demonstrates how to use interfaces to mock dependencies in tests. A `Datastore` interface allows a `MockDatastore` implementation to replace a real datastore, enabling isolated unit tests for the `Service` layer.
