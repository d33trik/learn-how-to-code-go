# Slice length and capacity

This exercise demonstrates how to use `make` to create a slice with a pre-allocated capacity, append another slice's elements to it, and inspect the result with `len` and `cap`. It also iterates over the slice using a traditional `for` loop by index.
