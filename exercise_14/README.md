# Infinite for loop and break

This exercise uses a `for` loop with no condition to create an infinite loop that prints integers from 0 to 10, then exits with `break`. It demonstrates unbounded iteration and loop control flow in Go.