# Variadic functions

This exercise demonstrates how to define variadic functions in Go using the `...` syntax and how to pass a slice to a variadic parameter with the spread operator. It covers summing integer values with a `for range` loop inside a variadic function and contrasts it with the equivalent slice parameter version.
