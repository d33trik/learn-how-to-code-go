# Pointer basics

This exercise declares a variable and prints both its value and memory address using the `&` operator. It introduces pointers in Go by demonstrating how to obtain and display the address of a variable with `fmt.Println`.
