# Closure

This exercise creates a closure by returning an anonymous function that captures and increments a variable from the enclosing scope. It demonstrates how closures retain access to variables from their lexical scope in Go.
