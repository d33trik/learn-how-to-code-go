# Range over a slice

This exercise declares a slice of integers and uses a `for range` loop to print each index and value. It demonstrates slice literals and the `range` keyword for iterating over collection types in Go.