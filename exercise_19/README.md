# Comma-ok idiom for maps

This exercise accesses a map using the comma-ok idiom to safely check whether a key exists before using its value. It demonstrates map lookup with the `ok` boolean and conditional branching based on key presence in Go.