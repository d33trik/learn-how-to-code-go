# Error handling with if err != nil

This exercise demonstrates how to handle errors in Go using the standard `if err != nil` pattern. A function returns an error, and the program checks for it before continuing, using `log.Fatalln` to exit if something goes wrong.
