# Constant and variable declaration

This exercise demonstrates how to declare a constant with `const`, a package-level variable with `var`, and a local variable with the short declaration operator `:=`.

It introduces the different declaration forms and their scopes in Go.
