# Slice literal and range

This exercise declares a slice of ice cream flavor names using a slice literal, prints its length and contents, then iterates over it with a `for range` loop to print each index and value. It practices slice literals, the `len` function, and range-based iteration in Go.
