# Slices of slices

This exercise declares a multi-dimensional slice of strings and uses nested `for range` loops to print each individual element. It introduces slice-of-slice literals and nested iteration over collection types in Go.
