# Creating custom errors with errors.New

This exercise demonstrates how to create custom error values in Go using `errors.New`. A package-level error variable is declared with `errors.New`, then returned from a function and handled by the caller with `if err != nil`.
