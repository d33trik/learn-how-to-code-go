# Pass by value vs pointer

This exercise defines a `person` struct and two functions that modify its `name` field. It demonstrates the difference between passing a struct by value, which requires returning the modified copy, and passing it by pointer with `*person`, which modifies the original directly.
