# Appending to a slice

This exercise demonstrates how to use the `append` function to add single elements, multiple elements, and all elements of another slice to an existing slice. It introduces the `...` syntax for spreading a slice into variadic arguments in Go.
