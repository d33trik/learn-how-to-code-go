# Function return values

This exercise demonstrates how to define functions that return values, including functions with multiple return values. It covers return type declarations in function signatures and the use of multiple assignment to capture returned values.
