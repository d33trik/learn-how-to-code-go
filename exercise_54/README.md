# Marshaling structs to JSON

This exercise defines a `User` struct and uses `json.Marshal` to convert a slice of structs into a JSON-encoded string. It demonstrates how Go serializes struct fields into JSON using the `encoding/json` package.
