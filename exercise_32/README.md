# Word frequency with a map

This exercise uses a map with `string` keys and `int` values to count the frequency of each word in a text. It demonstrates the `make` function for map creation and the use of maps as a counting data structure in Go.
