# Delete from a map

This exercise adds a new key-value pair to a map and then removes an existing one using the `delete` built-in function. It demonstrates how to insert and delete map entries in Go.
